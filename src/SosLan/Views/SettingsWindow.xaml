<Window x:Class="SosLan.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Paramètres - SOS-LAN"
        Height="320" Width="380"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        ShowInTaskbar="True">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Nom affiché sur le réseau" Margin="0,0,0,4" />
        <TextBox Grid.Row="0" x:Name="DisplayNameBox" Margin="0,22,0,0" />

        <TextBlock Grid.Row="1" Text="Touche déclenchant l'alerte" Margin="0,16,0,4" />
        <TextBox Grid.Row="1" x:Name="HotKeyBox"
                 Margin="0,38,0,0"
                 IsReadOnly="True"
                 Focusable="True"
                 Text="Cliquez ici puis appuyez sur une touche"
                 PreviewKeyDown="OnHotKeyPreviewKeyDown" />

        <TextBlock Grid.Row="2" Text="Durée de l'appui requise (secondes)" Margin="0,16,0,4" />
        <TextBox Grid.Row="2" x:Name="DurationBox" Margin="0,38,0,0" />

        <TextBlock Grid.Row="3" Text="Port réseau (identique sur tous les postes)" Margin="0,16,0,4" />
        <TextBox Grid.Row="3" x:Name="PortBox" Margin="0,38,0,0" />

        <TextBlock Grid.Row="4" x:Name="ErrorText" Foreground="Red" Margin="0,10,0,0" TextWrapping="Wrap" />

        <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Annuler" Padding="16,6" Margin="0,0,8,0" Click="OnCancelClick" />
            <Button Content="Enregistrer" Padding="16,6" Click="OnSaveClick" IsDefault="True" />
        </StackPanel>
    </Grid>
</Window>
